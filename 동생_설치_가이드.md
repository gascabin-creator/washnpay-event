# 워시앤페이 이벤트 자동 포인트 적립 시스템 — 설치 가이드
> 김포운양점 전용 | Claude Code를 이용한 단계별 설치

---

## 시스템 개요

손님이 문자를 보내면 → 사장님 폰(MacroDroid)이 감지 → 서버로 전달 → WashnPay에 자동 포인트 적립 → 손님에게 완료 문자 발송

```
손님 문자 전송
    ↓
사장님 폰 (MacroDroid)
    ↓ HTTP 요청
Cloud Run 서버 (Google)
    ↓ 자동 로그인
WashnPay 관리자 (포인트 적립)
    ↓ 완료 답장
손님 문자 수신
```

---

## 사전 준비물

| 항목 | 내용 |
|------|------|
| 안드로이드 폰 | 매장에 항상 켜두는 폰 (삼성 갤럭시 권장) |
| Google 계정 | Gmail 계정 하나 |
| WashnPay 관리자 계정 | 김포운양점 아이디/비밀번호 |
| PC | Windows/Mac (초기 세팅 시만 필요) |
| Claude Code | claude.ai/code 또는 Claude 앱 |

---

## STEP 1. 코드 파일 준비

PC에 작업 폴더를 만들고 아래 두 파일을 저장합니다.

### 📄 server.py

```python
"""
WashnPay 포인트 자동 적립 서버 — 김포운양점
"""
import re, subprocess, sys, threading, logging, time
from flask import Flask, request, jsonify

logging.basicConfig(level=logging.INFO, format="%(asctime)s %(levelname)s %(message)s")
log = logging.getLogger(__name__)

app = Flask(__name__)
SCRIPT_PATH = "point_auto.py"

SMS_PATTERN = re.compile(
    r"\[WNP\]([^/]+)/(가방[OX])/(\d{2,3}-?\d{3,4}-?\d{4})(?:/(.+))?"
)

REPLY_MESSAGES = {
    ("김포운양점", True):  "[워시앤페이] 5만원 입금확인, 포인트 6만원 충전완료. 감사합니다.",
    ("김포운양점", False): "[워시앤페이] 5만원 입금확인, 포인트 63,000원 충전완료. 감사합니다.",
}

# 중복 처리 방지 (5분 이내 동일 번호 재요청 무시)
_recent: dict = {}
_recent_lock = threading.Lock()
DEDUP_SECONDS = 300

def _run_script(phone_last4, amount, name=None, branch="김포운양점"):
    cmd = [sys.executable, SCRIPT_PATH, phone_last4, amount, name or "", branch]
    try:
        return subprocess.run(cmd, capture_output=True, text=True, encoding="utf-8", timeout=120)
    except subprocess.TimeoutExpired:
        return None

@app.route("/webhook", methods=["POST"])
def webhook():
    raw = request.get_data(as_text=True)
    log.info(f"[WEBHOOK] raw={raw!r}")

    data = request.get_json(silent=True) or {}
    sms_body = data.get("body", "") or raw.strip()
    log.info(f"[SMS_BODY] {sms_body!r}")

    m = SMS_PATTERN.search(sms_body)
    if not m:
        log.warning(f"[NO MATCH] sms_body={sms_body!r}")
        return f"WNP 형식 아님: {sms_body}", 200

    branch    = m.group(1).strip()
    bag       = m.group(2)
    full_phone = re.sub(r"-", "", m.group(3))
    cust_name  = (m.group(4) or "").strip() or None

    bag_needed  = (bag == "가방O")
    amount      = "60000" if bag_needed else "63000"
    phone_last4 = full_phone[-4:]

    # 중복 방지
    dedup_key = (branch, full_phone)
    now = time.time()
    with _recent_lock:
        if now - _recent.get(dedup_key, 0) < DEDUP_SECONDS:
            elapsed = int(now - _recent[dedup_key])
            log.warning(f"[DEDUP] {dedup_key} 중복 무시 ({elapsed}초 전)")
            return f"중복 요청 무시 ({elapsed}초 전)", 200
        _recent[dedup_key] = now

    reply = REPLY_MESSAGES.get(
        (branch, bag_needed),
        "포인트가 적립되었습니다. 워시앤페이를 이용해 주셔서 감사합니다!"
    )

    log.info(f"[PLAYWRIGHT] 시작: {phone_last4} {amount}원 {branch}")
    result = _run_script(phone_last4, amount, cust_name, branch)
    if result is None:
        log.error("[PLAYWRIGHT] 타임아웃")
    elif result.returncode == 0:
        log.info(f"[PLAYWRIGHT] 성공: {result.stdout.strip()}")
    else:
        log.error(f"[PLAYWRIGHT] 실패: {result.stderr.strip() or result.stdout.strip()}")

    return reply, 200, {"Content-Type": "text/plain; charset=utf-8"}

@app.route("/health")
def health():
    return jsonify({"status": "running"})

if __name__ == "__main__":
    import os
    port = int(os.environ.get("PORT", 5000))
    app.run(host="0.0.0.0", port=port, debug=False)
```

### 📄 point_auto.py

`server.py`와 같은 폴더에 저장합니다.  
**아래 3번째 줄 `ACCOUNTS`의 아이디/비밀번호를 김포운양점 실제 값으로 바꾸세요.**

```python
import sys, time, traceback, re
sys.stdout.reconfigure(encoding="utf-8")
sys.stderr.reconfigure(encoding="utf-8")
from playwright.sync_api import sync_playwright, TimeoutError as PlaywrightTimeoutError

ACCOUNTS = {
    "김포운양점": {"id": "여기에_WashnPay_아이디", "pw": "여기에_비밀번호"},
}
BASE_URL = "https://manager.washnpay.com"

def extract_last4(phone_text):
    token = phone_text.strip().split()[0] if phone_text.strip() else ""
    parts = token.split("-")
    if len(parts) >= 2:
        return parts[-1].strip()
    digits = re.sub(r"[^\d]", "", token)
    return digits[-4:] if len(digits) >= 4 else digits

def find_target_row(page, phone_last4, name=None):
    rows = page.locator("table tbody tr").all()
    if not rows:
        raise ValueError("검색 결과가 없습니다.")
    phone_matched = []
    for row in rows:
        cells = row.locator("td").all()
        if len(cells) < 3:
            continue
        last4 = extract_last4(cells[1].inner_text())
        if last4 == phone_last4:
            phone_matched.append((cells[2].inner_text().strip(), row))
    if not phone_matched:
        raise ValueError(f"전화번호 뒤 4자리 '{phone_last4}'에 해당하는 고객이 없습니다.")
    names_found = [n for n, _ in phone_matched]
    if name:
        name_matched = [(n, r) for n, r in phone_matched if n == name]
        if not name_matched:
            raise ValueError(f"뒤 4자리 '{phone_last4}' 중 이름 '{name}'인 고객이 없습니다.\n검색된 고객: {', '.join(names_found)}")
        cust_name, row = name_matched[0]
        print(f"    → 고객 확인: {cust_name}")
        return row
    if len(phone_matched) == 1:
        cust_name, row = phone_matched[0]
        print(f"    → 고객 확인: {cust_name}")
        return row
    raise ValueError(f"동일 뒤 4자리 고객 {len(phone_matched)}명. 이름을 지정해 주세요.\n검색된 고객: {', '.join(names_found)}")

def run(phone_last4, amount, name=None, branch="김포운양점"):
    account = ACCOUNTS.get(branch, list(ACCOUNTS.values())[0])
    with sync_playwright() as p:
        browser = p.chromium.launch(headless=True, args=["--no-sandbox", "--disable-dev-shm-usage"])
        page = browser.new_context().new_page()
        page.set_default_timeout(15000)
        try:
            print(f"[1/5] 로그인 중... ({branch})")
            page.goto(BASE_URL)
            page.wait_for_load_state("networkidle")
            page.fill("input[placeholder='Username']", account["id"])
            page.fill("input[placeholder='Password']", account["pw"])
            for chk in page.locator("input[type='checkbox']").all():
                if not chk.is_checked():
                    chk.check()
                    try:
                        modal = page.locator(".modal.show, .modal.d-block")
                        if modal.is_visible(timeout=1000):
                            page.click("button:has-text('확인')")
                            time.sleep(0.3)
                    except Exception:
                        pass
            page.click("button:has-text('Login')")
            page.wait_for_load_state("networkidle")

            print("[2/5] 고객관리 메뉴 이동 중...")
            page.click("text=고객관리")
            time.sleep(0.3)
            page.click("text=회원관리")
            page.wait_for_load_state("networkidle")
            time.sleep(0.3)

            label = f"{phone_last4}" + (f" / {name}" if name else "")
            print(f"[3/5] 검색 중... ({label})")
            page.fill("input[placeholder='번호 or 이름 입력']", phone_last4)
            page.click("button:has-text('검색')")
            page.wait_for_load_state("networkidle")
            try:
                page.wait_for_selector("table tbody tr", timeout=8000)
            except Exception:
                pass
            time.sleep(0.5)

            print("[4/5] 대상 고객 검색 및 포인트 적립 버튼 클릭 중...")
            target_row = find_target_row(page, phone_last4, name)
            before_pts = target_row.locator("td").nth(4).inner_text().strip()
            target_row.locator("button:has-text('포인트 적립')").click()
            time.sleep(0.3)

            print(f"[5/5] 팝업 입력 중... (금액={amount})")
            modal = page.locator(".modal.show, .modal.d-block").first
            modal_inputs = modal.locator("input[type='text']")
            reason = "이벤트 5만원 충전 (가방포함)" if amount == "60000" else "이벤트 5만원 충전 (가방미포함)"
            modal_inputs.nth(1).click()
            modal_inputs.nth(1).press_sequentially(amount, delay=30)
            modal_inputs.nth(2).click()
            modal_inputs.nth(2).press_sequentially(reason, delay=30)
            time.sleep(0.3)
            modal.locator("button:has-text('저장')").click()
            page.wait_for_load_state("networkidle")
            time.sleep(1.0)

            page.fill("input[placeholder='번호 or 이름 입력']", phone_last4)
            page.click("button:has-text('검색')")
            page.wait_for_load_state("networkidle")
            time.sleep(0.8)
            verify_row = find_target_row(page, phone_last4, name)
            after_pts = verify_row.locator("td").nth(4).inner_text().strip()
            diff = int(re.sub(r"[^\d]", "", after_pts) or "0") - int(re.sub(r"[^\d]", "", before_pts) or "0")
            if diff == int(amount):
                print(f"\n완료! {label} 고객 {before_pts} → {after_pts} (+{amount} 적립 확인)")
            else:
                raise ValueError(f"적립 검증 실패: 기대 +{amount}, 실제 {before_pts}→{after_pts}")
        finally:
            browser.close()

if __name__ == "__main__":
    if len(sys.argv) < 3:
        print("사용법: python point_auto.py <전화번호뒤4자리> <포인트금액> [이름] [지점]")
        sys.exit(1)
    _, phone, amount = sys.argv[:3]
    cust_name = sys.argv[3] if len(sys.argv) > 3 and sys.argv[3] else None
    branch_arg = sys.argv[4] if len(sys.argv) > 4 else "김포운양점"
    try:
        run(phone, amount, cust_name, branch_arg)
    except Exception as e:
        print(f"\n최종 오류: {e}", file=sys.stderr)
        traceback.print_exc(file=sys.stderr)
        sys.exit(1)
```

### 📄 requirements.txt

```
playwright==1.60.0
flask==3.1.0
gunicorn==23.0.0
```

### 📄 Dockerfile

```dockerfile
FROM mcr.microsoft.com/playwright/python:v1.60.0-noble
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
RUN pip install gunicorn
RUN playwright install chromium --with-deps
COPY server.py point_auto.py ./
ENV PORT=8080
CMD exec gunicorn --bind :$PORT --workers 1 --threads 4 --timeout 0 server:app
```

---

## STEP 2. WashnPay 아이디/비밀번호 입력

`point_auto.py` 파일 열고 이 부분을 수정합니다:

```python
ACCOUNTS = {
    "김포운양점": {"id": "실제_아이디", "pw": "실제_비밀번호"},
}
```

> WashnPay 관리자(manager.washnpay.com)에 로그인할 때 쓰는 아이디/비밀번호입니다.

---

## STEP 3. Google Cloud Run 서버 배포

### 3-1. gcloud CLI 설치

1. [https://cloud.google.com/sdk/docs/install](https://cloud.google.com/sdk/docs/install) 에서 설치
2. 터미널(명령 프롬프트)에서 실행:
   ```
   gcloud init
   ```
3. Google 계정으로 로그인

### 3-2. 프로젝트 생성

```bash
gcloud projects create washnpay-gimpo --name="WashnPay Gimpo"
gcloud config set project washnpay-gimpo
gcloud services enable run.googleapis.com cloudbuild.googleapis.com
```

### 3-3. 배포

파일 4개(server.py, point_auto.py, requirements.txt, Dockerfile)가 있는 폴더에서:

```bash
gcloud run deploy washnpay-event \
  --source . \
  --region=asia-northeast3 \
  --allow-unauthenticated \
  --project=washnpay-gimpo
```

배포 완료 후 **서버 URL**이 출력됩니다. 예:
```
https://washnpay-event-XXXXXXXXXX.asia-northeast3.run.app
```

이 URL을 메모해 두세요. MacroDroid에서 사용합니다.

> ⚠️ 위 과정이 어려우면 Claude에게 "gcloud 설치부터 도와줘"라고 물어보세요.

---

## STEP 4. 매장 폰에 MacroDroid 설치

### 4-1. Samsung Messages RCS 끄기 (필수!)

Samsung Galaxy 폰은 기본적으로 RCS(채팅) 메시지를 사용하는데, MacroDroid는 RCS를 감지하지 못합니다. 반드시 꺼야 합니다.

1. **메시지** 앱 열기
2. 우측 상단 **⋮** → **설정**
3. **채팅+(RCS) 설정**
4. **채팅 기능 사용** → **끄기(OFF)**

### 4-2. MacroDroid 설치 및 설정

1. Play 스토어에서 **MacroDroid** 설치
2. 앱 열고 **매크로 추가(+)** 버튼

### 4-3. 매크로 구성

**① 트리거 (Trigger) 설정**
- 트리거 추가 → **문자 메시지(SMS) 수신**
- 보낸 사람: **모든 번호**
- 포함 단어: `[WNP]`

**② 액션 (Action) 설정 — 2개 추가**

**액션 1: HTTP 요청**
- 액션 추가 → **네트워크** → **HTTP 요청**
- URL: `https://washnpay-event-XXXXXXXXXX.asia-northeast3.run.app/webhook` (3-3에서 받은 URL)
- 메서드: **POST**
- Content-Type: `text/plain`
- Body: `{sms_body}`
- 응답 저장 변수명: `reply_response`

**액션 2: 문자 발송**
- 액션 추가 → **메시지** → **문자 메시지 보내기**
- 수신 번호: `{sms_number}`
- 내용: `{lv=reply_response}`

**③ 조건(Constraint): 없음**

**④ 이름 설정**: `워시앤페이 이벤트`

**⑤ 저장**

---

## STEP 5. 테스트

### 5-1. 손님 문자 형식 안내

손님에게 아래 형식으로 문자를 보내달라고 안내합니다:

```
[WNP]김포운양점/가방O/손님전화번호/손님이름
```
또는
```
[WNP]김포운양점/가방X/손님전화번호/손님이름
```

- `가방O` → 포인트 **60,000원** 충전
- `가방X` → 포인트 **63,000원** 충전

**예시:**
```
[WNP]김포운양점/가방O/010-1234-5678/홍길동
```

### 5-2. 테스트 문자 발송

다른 폰으로 매장 폰 번호에 위 형식으로 문자를 보냅니다.
(단, 손님 전화번호는 WashnPay에 **실제 가입된 번호**여야 합니다.)

### 5-3. 성공 확인

- 손님 폰: 완료 문자 수신
- WashnPay 관리자: 포인트 적립 이력 확인

---

## 자주 발생하는 문제

| 증상 | 원인 | 해결 |
|------|------|------|
| 문자 보냈는데 아무 반응 없음 | RCS 꺼져있지 않음 | STEP 4-1 반복 |
| 완료 문자는 왔는데 포인트 미적립 | WashnPay 아이디/비밀번호 틀림 | point_auto.py ACCOUNTS 확인 |
| "검색 결과가 없습니다" 오류 | 손님이 WashnPay 미가입 | 손님 앱 가입 후 재요청 |
| 포인트가 2번 적립됨 | 중복 문자 발송 | 5분 이내 자동 차단됨 (정상) |

---

## 문제 해결 시 Claude에게 물어볼 것들

- `gcloud` 명령어 오류가 나요
- MacroDroid에서 HTTP 요청 설정을 어떻게 하나요
- 배포 후 서버 URL이 어떻게 나오나요
- Cloud Run 로그를 보고 싶어요

Claude Code 앱을 열고 이 가이드 파일(`동생_설치_가이드.md`)이 있는 폴더를 프로젝트로 열어서 질문하면 됩니다.

---

*워시앤조이 이벤트 자동화 시스템 | 문의: 형/오빠*
